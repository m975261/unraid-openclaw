# Unraid Templates for openclaw

XML templates for Unraid Community Applications.

## Install
Copy `my-openclaw.xml` to `/boot/config/plugins/dockerMan/templates-user/` on Unraid,
then: Docker tab → Add Container → select from template list.
